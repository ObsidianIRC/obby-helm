{{- if .Values.obbyApi.enabled -}}
{{- range $name, $cfg := .Values.obbyApi.persistence }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "obby.fullname" $ }}-obby-api-{{ $name | kebabcase }}
  labels:
    {{- include "obby.componentLabels" (dict "component" "obby-api" "Release" $.Release "Chart" $.Chart "Values" $.Values) | nindent 4 }}
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: {{ $cfg.size }}
  {{- if $cfg.storageClass }}
  storageClassName: {{ $cfg.storageClass | quote }}
  {{- end }}
{{- end }}
{{- end }}
